#!/usr/bin/env node

const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

console.log('🔧 Setting up Git hooks...');

const hooksDir = path.join(__dirname, '..', '.git', 'hooks');
const preCommitHook = path.join(hooksDir, 'pre-commit');

try {
  // Проверяем, что мы в git репозитории
  if (!fs.existsSync(hooksDir)) {
    console.error('❌ Not a git repository. Please run "git init" first.');
    process.exit(1);
  }

  // Создаем pre-commit hook
  const hookContent = `#!/bin/sh
# Pre-commit hook for automatic version bumping
# This file is automatically generated. Do not edit manually.

cd "${path.resolve(__dirname, '..')}"
node "scripts/pre-commit.js"
`;

  fs.writeFileSync(preCommitHook, hookContent, 'utf8');
  
  // Делаем hook исполняемым
  execSync(`chmod +x "${preCommitHook}"`, { stdio: 'inherit' });
  
  console.log('✅ Pre-commit hook installed successfully!');
  console.log(`📁 Hook location: ${preCommitHook}`);
  console.log('');
  console.log('📋 What happens now:');
  console.log('  • Every commit will automatically bump the patch version');
  console.log('  • Build info will be updated with new version');
  console.log('  • Both package.json and build-info.ts will be added to commit');
  console.log('');
  console.log('🚀 Try making a commit to see it in action!');
  
} catch (error) {
  console.error('❌ Failed to setup git hooks:', error.message);
  process.exit(1);
}
